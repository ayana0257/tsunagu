$("#comments_area").html("<%= j(render 'index', { comments: @comment.blog.comments }) %>")
$("textarea").val('')

$(function() {
 $('a[data-method="delete"]').on('ajax:success', function(xhr,data,status) {
    $('#comment_'+data.comment.id).fadeOut();
 });
});