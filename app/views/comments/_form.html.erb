
<%= form_with(model: [blog, comment] ) do |form| %>
  <div>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする", class: "btn btn-default btn-ms" %>
  </div>
<% end %>